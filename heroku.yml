setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python app/manage.py collectstatic --noinput
run:
  web: gunicorn app/django_app.wsgi
